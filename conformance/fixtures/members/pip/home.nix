# The module pip's cli mode points at. Deliberately empty: the member-set derivation never forces
# it (`configuration` is a deferredModule), and this suite has no home-manager to evaluate it with.
{ }
