# The member's home, for the ADR-0020 pair. Nothing in the member-set derivation reads it — it
# keys on identity.json — so it stays empty: the fixture exists to be a faithful user
# directory, not a home to evaluate (that is examples/users' job, where home-manager lives).
{ }
